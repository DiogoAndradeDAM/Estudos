/* Função para listar itens de um array //

const fruits = ["Banana", "Orange", "Apple", "Mango"];

let text = "<ul>";
fruits.forEach(myFunction);
text += "</ul>";

function myFunction(value) {
  text += "<li>" + value + "</li>";
} 
*/

/* forEach (explicação)
 Serve como um enumerate do python

 const nums = [1, 2, 3, 4, 5, 6];

nums.forEach(num => {
  console.log(num);
})
*/
