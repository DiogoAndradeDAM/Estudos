#include <stdio.h>
#include <stdlib.h>

int main(){
  //Para leitura de caracteres temos a seguintes opções
  //scanf() com %c e permite a entrada de multiplos valores
  //getchar()
  //getc(stdin) está função é usada para ler texto de vários modos e stdin é para ler o teclado
  //fgetc(stdin) serve para ler textos de arquivos, o stdin funciona igual ao acima

  //Cuidado! os tipo char lê inclusive comandos do teclado como espaços e enter, uma forma de contornar isto é dar um espaço antes de %c
  return 0;
}